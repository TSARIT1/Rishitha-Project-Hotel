import PlaceholderPage from '../components/UI/PlaceholderPage';

const Tables = () => {
  return (
    <PlaceholderPage 
      title="Table & QR Code Management"
      description="Generate QR codes for tables, manage table status, track occupancy, and enable contactless menu access for customers. Print QR code labels for each table."
    />
  );
};

export default Tables;
